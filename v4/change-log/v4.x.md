---
description: 本文描述了Kuboard_v4.x的版本变更说明
---

# Kuboard v4.x 变更日志

Kuboard v4 版本更新，如果您已经正常安装了 Kuboard v4.x (以 v4 开头的版本号，例如 kuboard v4.0.0.0 / kuboard v4.0.0.1 / kuboard v4.2.1.0 等)，升级到 v4.x 更新的版本时，只需要修改镜像的标签即可。

<!--

6、监控套件-1.33版本
10、loki套件部署后loki服务总是重启
13、还有节点标签污点添加页面需求
14、1.33.5集群版本的监控套件

待修复问题：
  - 如果某个集群的 apiserver 没有响应时，不能正常打开列表页，原因是：
    - 调用此接口时一直没有响应： http://localhost:8848/api/cluster.kuboard.cn/v4/cluster-cache/cluster-namespace-tree?apiGroupName=&resource=nodes&namespaced=false

-->

## v4.2.0.2

 发布日期： 2026-08-12

 - 问题修订：
  - 修复开启「强制绑定 MFA」时的登录与页面访问流程
    - 未绑定 MFA 的用户登录后可正常获得 token，并自动跳转到 MFA 绑定页完成绑定（不再直接登录失败）
    - 路由守卫在用户未绑定时，仅允许访问登录页与 MFA 绑定页，其余页面自动重定向到绑定页
    - MFA 绑定成功后自动刷新用户信息，避免被路由守卫立即踢回绑定页
    - 绑定接口不再强制要求输入当前密码（登录 token 已作为合法凭证）

## v4.2.0.1

 发布日期： 2026-08-06

 - 问题修订：
  - 使用 service account 导入的集群，在打开对象详情页时提示 401，用户会话已过期的错误。

## v4.2.0.0

发布日期： 2026-08-03

- 新特性：
  - MFA（TOTP）登录认证功能
    - 支持 TOTP 多因素认证登录
    - 支持恢复码
    - 管理员可在系统配置中启用/禁用 MFA
    - 在 Avatar 下拉菜单中增加 MFA 绑定入口
    - 优化 MFA 绑定页面布局与引导文案
    - 使用 qrcode-generator 库替换手写 QR 码生成
  - Gateway API 资源管理
    - 新增 GatewayClass 资源管理（Gateway API 入口）
    - 新增 Gateway API 全部 7 种资源的管理视图（Gateway、HTTPRoute、GRPCRoute、TCPRoute、UDPRoute、TLSRoute、ReferenceGrant）
    - 改进 GatewayClass 表单向导体验
    - 新增 IngressClass 集群级资源管理页面
    - 新增服务与路由菜单
  - Ingress 表单创建/编辑/详情页
    - spec.ingressClassName 由纯文本输入升级为可创建可筛选的下拉选择器
  - PV 表单创建与卷源配置支持（NFS、HostPath、Local 等）
  - 资源可用性治理：当 K8s 扩展组件未安装时，在界面提示用户
  - 节点列表页增加行展开模式，以折叠不常用列
  - 资源全景图（Resource Map）
    - 新增 K8s 资源全景视图，以图形化方式展示集群内所有资源及其关联关系
    - 支持按命名空间、资源类型筛选
    - 支持资源详情速览面板（Glance）
    - 支持连通子图折叠与展开
    - 支持节点名称超长截断与 tooltip 提示
    - 支持 JobSet、Events、CR owner 等关联关系
    - 支持详情/编辑页跳转链接
  - Node Shell 和 Debug Container 支持
    - 新增节点命令行界面（Node Shell）
    - 支持向 Pod 注入临时调试容器（Ephemeral Debug Container）
    - 支持 Shell 会话管理与定时清理
  - Helm Release 管理功能
    - 新增 Helm Chart 市场管理（Marketplace），支持 repo 管理、chart 搜索与详情查看
    - 新增 Helm Release 安装/升级/回滚功能
    - 安装/升级/回滚进度支持 SSE 事件重放，关闭页面后可重新查看
    - 新增 Helm Revision diff 对比功能
    - 新增 Helm Schema 表单编辑与 cancel 操作
    - 新增 Helm 事件页面（events），实时展示 Helm 操作日志
    - Helm 卸载改为详情页弹窗确认 + 跳转 events 页
    - 为 Helm 页面添加 i18n 国际化支持
    - 新增 chart 详情端点，展示 icon/maintainers/dependencies 等信息
    - kubeconfig server 改用 HTTPS 修复 chart lookup 401 错误
  - 统一 K8s 版本兼容性感知层（K8sCapability）
    - 后端三层探源：静态版本表、预探 /apis、事件驱动
    - 前端 useK8sCapability hook，提供 feat/apiVersion/cacheable/can/version 五个 API
    - 覆盖 policy.eviction、flowcontrol.flowschemas、portforward.protocol 等能力标记
  - Service 表单重构
    - 重构为 el-tabs 布局，分端口、选择器、高级设置三个标签页
    - 优化选择器交互体验
  - K8s 对象处理机制重构（Stage 1）
    - 重构 HandleK8sObjectDialog 组件
    - 重构 HandleDeleteDialog 组件
    - 重构 KubeApiController 后端接口
    - 增加请求取消机制（abortUtils）
  - TreeFilter 树节点文本截断 + tooltip 全名提示
  - Avatar 下拉菜单增加 MFA、所属分组、权限信息、访问密钥入口

- 优化：
  - 用户列表行下展开重构
  - 优化 Ingress 表单样式
  - 优化 PV 表单
  - 资源类型不存在时的提示消息优化
  - 优化菜单项
  - 完善 kb-page-main 样式
  - 优化容器组列表页体验
  - 优化构建脚本和部署脚本，修订构建过程中的编译相关问题

- 问题修订：
  - 修正 Kuboard 套件导航路径使用无效的 /kubernetes/ 前缀
  - 修复 StorageClass 详情/编辑页缺失 accessories 时的 404 错误
  - 修复创建访问密钥失败的问题
  - 修复 ClusterCacheController 请求参数的问题
  - 修复 inject() 在模块顶层调用、DebugContainerButton pod prop 为 undefined、KbInfoItem value prop 缺失的三个运行时警告
  - 修复 ToolBarLeft 放宽 K8s 对象名正则字符集支持含点，并新增 Helm 面包屑

## v4.1.0.3

发布日期： 2026-04-12

- 优化：
  - Pod 详情页显示 Pod 上一次的状态信息（Last State）
  - 异常工作负载显示在列表的前面
  - 集群详情页显示代理 URL
  - 事件列表页移除"新增"按钮

- 问题修订：
  - 普通用户无法修改主题的问题
  - 修订 Addon 的一个 BUG
  - 修订创建集群时默认情况下不传递 timeZone 的问题
  - 解决 Playwright 并行执行时出现的 StaleLogin 问题

## v4.1.0.2

发布日期： 2026-03-21

- 新特性：
  - 增加全局终端主题配置，管理员可以在系统配置中自定义全局终端主题，包括字体大小、字体颜色、行间距、主题类型、主题名称、终端日志行数等。
  - 用户可以自定义自己的终端主题，默认使用全局终端主题。

- 优化：
  - 增加历史表 kb_cluster_sync_data_deleted
  - 增加 Service 的创建、编辑表单，完善 Service 的显示表单

- 问题修订：
  - 解决部分浏览器下使用 crypto.randomUUID 函数出现不兼容的问题
  - 解决部分情况下，与 mysql 数据库不兼容的问题

## v4.1.0.1

发布日期： 2026-03-15

- 优化：
  - 工作负载列表页（Deployment、StatefulSet、DaemonSet、Pod）使用 Tag 显示条目状态
  - 日志界面增加关键字搜索功能
  - 为 kb_cluster_sync_data.cluster_sync_task_id 字段增加索引
  - 优化 pod.status.conditions 的显示
  - 在集群信息中配置所在时区，并以集群所在时区显示时间戳

- 问题修订：
  - 修复节点列表页不能显示节点资源利用率的问题
  - 文件浏览器上传文件时可能失败的问题
  - 修改角色权限时的一个操作便利性问题

## v4.1.0.0

发布日期： 2026-03-08

- 新特性：
  - SSE 机制（Server Side Event）
    - 在服务端 K8S 对象发生变化时，通过 SSE 机制将最新变化实时更新到页面端
      - Deployment 详情页
      - StatefulSet 详情页
      - DaemonSet 详情页
      - Pod 详情页
    - 支持单节点 Kuboard 部署（使用内存队列发布事件）
    - 支持多节点 Kuboard 高可用部署（使用 Redis pub/sub 在所有实例间发布事件）
    - 用户可以在浏览器端禁用 SSE，此时退回到定时刷新页面的模式获取页面的更新内容
  - 增加便捷操作
    - 批量重启
    - 批量调整副本数
    - 批量修改镜像版本
  - K8S 对象（例如 ConfigMap、Deployment、StatefulSet、DaemonSet 等）详情页增加 K8S 对象历史版本查询功能
    > （仅支持查询在 Kuboard 中执行的版本变更历史）
- 优化：
  - 列表页增加集群、名称空间新的筛选模式
  - 列表页创建对象时，默认选中当前过滤器中第一个选中的集群、名称空间
  - 容器组详情页面增加跳转到归属对象的链接
  - 升级到 OpenJDK 25.0.2
  - 完善操作审计查询功能
    - 增加查询条件：操作对象名称
    - 操作类型可以多选，可以清空查询条件
  - 登录用户均可访问 kuboard 名称空间的 http:metrics-scraper:8000 接口的 get 方法
  - 日志追踪界面可以暂停滚动
  - 优化缓存表查询性能
- 问题修订：
  - 下载日志时，进度条显示不准确的问题以及下载文件不完整的问题
  - 容器组列表页，当容器组包含多个容器时，必须先选择容器，才能进入日志、终端界面
  - 使用表单创建 DaemonSet 时，对象类型不正确的问题
  - 如果浏览器日期跟服务器日期出现不一致时，Kuboard 在一天中特定时段提示 `缺少授权文件` 的问题
  - 执行 cd.kuboard.cn 分组下的接口时，审计日志不能记录请求报文的问题
  - 当角色关联到空的用户组时，编辑角色权限失败的问题
  - 更新集群的 token 时，需要重启服务才能生效的问题
  - 工作负载编辑页面快速创建 PVC 时不显示保存按钮的问题
  - 普通用户在导出资源时不能选择其他对象的问题

## v4.0.1.0

发布日期： 2026-01-03

- 新特性：
  - 可以导出 K8S YAML 文件

- 优化：
  - 优化日志文件目录
  - Deployment/StatefulSet/DaemonSet 页面显示其关联的事件

- 问题修订：
  - 使用外部用户登录时，不能正常进入终端界面的问题
  - 工作负载详情页面，预览并编辑 YAML 时出错的问题
  - 在工作负载编辑页面，配置 DNS Config 时出错的问题

## v4.0.0.1

发布日期： 2025-11-30

- 优化：
  - Cookie 操作优化
  - 颜色主题为 system 情况下，首页加载页背景始终为黑色的问题
  - PVC 列表页显示 storageClass、phase

- 问题修订：
  - Deployment/StatefulSet/DaemonSet 列表页快速进入容器组的 日志/终端界面 时，如果容器组存在多个容器的情况下出错的问题
  - 在工作负载编辑页面快速创建 Secret 时，不能保存的问题
  - 在工作负载编辑页面快速创建 ConfigMap 时，不能保存的问题
  - 在工作负载编辑页面选择 imagePullSecrets 时，条目应该为 `[ { name: secretName } ]`，而不是 `[ secretName ]`
  - 在工作负载编辑页面，如果该工作负载已经设置了 imagePullSecrets 时，编辑界面报错的问题
  - 在工作负载编辑页面，如果用户没有集群级别权限，打开节点调度策略的节点（标签）选择对话框出错的问题
  - 添加存储类报错的问题

## v4.0.0.0

发布日期： 2025-10-12

- 新特性：
  - 终端界面可以选择字符编码（当容器内部的编码方式不是 UTF-8 时，切换字符编码可以避免终端显示的乱码）
  - 文件浏览器可以选择字符编码（当容器内部的编码方式不是 UTF-8 时，切换字符编码可以避免文件浏览器显示的乱码）
    - 容器内部使用的编码方式不是 UTF-8 时，Kuboard 文件浏览器的已知问题：
      - 文件列表中的文件名不能正常显示
      - 文本文件编辑界面不能保存
  - 日志界面可以选择字符编码
    - 已知问题：由于 kubernetes pods/logs 接口返回的编码已经不是原编码格式，日志界面不能正常还原汉字的原编码。

- 优化：
  - 在用户列表页/详情页增加解锁账号的操作
  - 容器组列表页增加日志/终端按钮
  - Deployment/StatefulSet/DaemonSet 列表页增加日志/终端按钮

## v4.0.0.0-rc.07

发布日期： 2025-07-13

- 新特性：
  - 配置 Logo、标题等信息
- 优化：
  - 从详情页返回列表页时，自动回到之前的翻页
- 问题修订：
  - 使用 MySQL5.7 版本时 kuboard-server 启动失败的问题
  - Deployment 详情页查询 ReplicaSet 时未限定 labelSelector 的问题
  - Kuboard 套件不能正常激活、使用的问题

## v4.0.0.0-rc.06

发布日期： 2025-06-01

- 新特性：
  - 可以设置 `v1/events` 对象在 K8S 中删除后 Kuboard 缓存保留该对象的时长，默认为 168 小时。
  - 如果有新版本的 Kuboard，在界面左下角弹出提示信息
- 优化：
  - 通过环境变量 `TZ` 设置日志记录里的时区，默认为 Asia/Shanghai
  - 跟随系统设置暗黑主题
  - 优化 safari 浏览器中的字体样式
  - 查询名称空间列表时，只包含用户具备名称空间内 pods/list 权限的名称空间
  - 设置默认的只读角色
  - 必须具备 pods/exec 的 create 权限才可以访问终端界面
  - 套件安装
    - 优化套件容器组状态检查界面
    - 安装套件自动创建 Kuboard 名称空间
  - 批量处理 K8S 对象的对话框中增加进度条，并优化取消按钮功能
  - 完善系统配置界面的按钮鉴权
- 问题修订：
  - 修订创建名称空间出错的问题
  - 在 /audit/events 页面刷新整个页面时，不能正常打开页面的问题
  - 在 /audit/events 页面查询超出日期范围的事件时，显示 SQL 错误的问题
  - 修订 CICD 对话窗口跳转到访问密钥页面的链接错误的问题
  - 修订无缓存列表的鉴权问题
  - 调用 K8S Post 接口时的日志级别设置问题

## v4.0.0.0-rc.05

发布日期： 2025-04-20

- 新特性：
  - 将集群信息同步日志记录到单独的日志文件中
  - 增加审计日志功能
    - 记录到数据库，并查询审计历史
    - 记录到文件
  - 增加密码策略配置选项
    - 配置会话超时时间
    - 配置尝试次数及账号锁定时间
    - 配置不重复历史密码数量
  - 优化登录页面，增加 to 参数，指定完成登录后跳转的目标页面
- 问题修订：
  - 不能正常授权菜单（设置/系统设置，设置/授权文件）的问题
  - Deployment 页面显示副本数错误的问题

## v4.0.0.0-rc.04

发布日期： 2025-03-22

- 新特性：
  - 支持通过 http proxy 访问 kube-apiserver
  - 修改集群的导入信息
  - 新增网络策略（networking.k8s.io/v1/networkpolicies）的列表页、详情页、创建页、编辑页
  - 新增名称空间资源配额（ResourceQuota）、限制范围（LimitRange）管理
- 优化：
  - 从 YAML 创建对象时，默认删除 `.metadata.creationTimestamp`, `.metadata.generation`, `.metadata.resourceVersion`, `.metadata.uid` 字段
- 问题修订：
  - 部分情况下，不能正常获取集群版本信息的问题

## v4.0.0.0-rc.03

发布日期： 2025-02-19

- 新特性：
  - 兼容 V3 授权文件
  - 系统设置页面添加密码有效期相关设置，添加密码策略相关设置
  - 支持对 StatefulSet 进行伸缩操作
  - 支持对 Deployment/ StatefulSet 进行水平自动伸缩（Horizontal Pod Auto Scaler），兼容 autoscaling/v1, autoscaling/v2
  - 可以动态修改集群缓存同步任务的日志级别
  - 容器组详情页、节点详情页，显示近 30 分钟的资源使用情况曲线图
- 优化：
  - 支持在 YAML 对话框中包含多个对象
  - 优化 Kubernetes 对象详情页、创建页、编辑页的布局
  - 优化 ConfigMap、Secret 编辑页的布局
  - 列表页记住上次选择的分页大小
  - 点击事件列表时，如果 Kuboard 中没有关联对象对应的详情页，则提示用户，而不是跳转到 404 页面
  - 集群列表页按名称排序
  - 用户名中可以包含 `.` `-` `_`
  - 稳定性
    - 文件浏览器中文件列表大小超过 100 时，页面刷新缓慢的问题
    - 优化 /k8s-api 反向代理的异步处理能力
    - 优化访问 k8s 接口时，HTTP 连接的线程管理能力
- 问题修订：
  - 解决日志套件界面不能正确打开的问题
  - 解决部分情况下（kubectl api-resources 命令结果中带有错误信息时）不能完成集群同步的问题
  - 解决重新导入集群时，未更新 `集群名称->集群ID` 缓存的问题
  - 解决部分情况下，不能找到 ResourceName 的问题
  - 解决使用 MySQL 5.7 时，不能打开名称空间详情页的问题
  - 解决集群失联状态下，Kuboard 中部分功能异常的问题
  - 终端界面切换 shell 类型失败的问题
  - 解决文件浏览器下载文件时，如果文件较大（例如，超过 200M），有较大几率下载结果大小与原文件大小不一致的问题

## v4.0.0.0-rc.02

发布日期： 2024-12-22

- 新特性：
  - 支持容器文件浏览器
- 优化：
  - 进入容器终端界面时，不应自动添加三个回车符
  - 菜单采用 Column 布局时，部分情况下不能正常显示 Column 的情况
- 问题修订：
  - 修订不能对定制资源菜单进行鉴权的问题

## v4.0.0.0-rc.01

发布日期： 2024-11-03

- 新特性：
  - 支持高可用部署（需要使用 Redis 做分布式缓存）
- 优化：
  - 导入工作负载页面：可以快速创建名称空间
  - 容器组编辑页面：优化容器端口名称的校验规则
- 问题修订：
  - 解决集群对象信息同步时存在的内存泄露问题
  - 解决日志界面关闭后，服务端 http 长连接未关闭的问题
  - 解决终端界面关闭后，服务端 websocket 连接未关闭的问题

## v4.0.0.0-beta.05

发布日期： 2024-10-05

- 新特性：
  - 任务 Job 列表页、详情页、编辑页、
  - 定时任务 CronJob 列表页、详情页、编辑页、创建页
  - 支持访问密钥
  - 支持持续交付接口
  - 可以查看 Kuboard 接口文档
- 优化：
  - 鉴权优化：
    - 容器组详情：必须同时具备 pods/exec 的创建、查看权限才可以进入容器组的终端界面
    - 列表页：在没有所有名称空间权限时，优化提示信息
  - Secret 编辑/创建页面的 dockerconfigjson 模式下，可以删除 server 条目
  - 节点列表页
    - 显示 Roles、Version、Internal IP、OS Image、Kernel Version、Container Runtime 等字段
    - 集群未安装 metrics-server 时，优化提示信息
  - 优化列表页的默认选择范围判定方式
  - 优化授权失败时的提示信息
  - 更新依赖包到最新版本
    - spring-boot 3.3.4
    - vue 3.5.10
    - element-plus 2.8.4
- 问题修订：
  - 工作负载详情：dropdown 菜单的鉴权错误

## v4.0.0.0-beta.04

发布日期： 2024-08-18

- 新特性：
  - 从表单创建名称空间
- 优化：
  - 使用 kubeconfig 导入集群时，校验 context 不能为空
  - 列表页输入检索参数后，敲击回车按钮触发搜索
  - 列表页 input 框输入内容后，可以清空
  - 定制资源定义列表页：点击定制资源条目跳转到定制资源列表页
  - 节点列表页显示节点资源占用情况统计
  - 完善名称空间详情页的概览显示
- 问题修订：
  - 手风琴菜单模式下，二级菜单包含子条目时不显示标题的问题
  - 事件列表页面，当事件关联的对象为节点时，点击节点名称不能跳转到节点页面的问题
  - 集群详情页同步状态标签刷新按钮无效的问题
  - 从 YAML 创建名称空间时，提示 502 错误的问题

## v4.0.0.0-beta.03

发布日期： 2024-08-04

- 新特性：
  - 显示 Service 详情页
  - 显示名称空间详情页
  - 支持导入 K8S 对象
  - 支持安装 Kuboard 套件（兼容 kuboard v3 版本的套件）

- 优化：
  - K8S 对象操作对话框中，只在 delete 操作时要求确认
  - 容器组详情页中，可以跳转到所在节点的详情页

- 问题修订：
  - 存在失联集群时，不能显示角色详情页中可选权限列表的问题；
  - 事件列表页鉴权错误；
  - 修订 K8S apply 操作的逻辑；

## v4.0.0.0-beta.02

发布日期： 2024-07-07

- 新特性：
  - 支持 Service 端口的 KuboardProxy
  - 支持 Pod 端口的 KuboardProxy
  - 工作负载编辑器的容器组模板中，定义数据卷时支持如下类型数据卷：
    - NFS
    - HostPath
    - 配置字典
    - 密文

- 优化：
  - 容器组列表页增加 Ready、Phase、Restarts 字段
  - 创建 PVC 时， spec.accessModes 字段为必填字段
  - 对象修改界面，确认操作从输入 `CONFIRMED` 改为 `OK`

## v4.0.0.0-beta.01

发布日期： 2024-06-15

- 新特性：
  - 推送到镜像仓库 swr.cn-east-2.myhuaweicloud.com/kuboard/kuboard:v4
- 优化：
  - 在浏览器记住列表页中的 ColumnSetting
- 问题修正：
  - 修订重置密码后，用户不能使用默认密码登录的问题

## v4.0.0.0-alpha.03

发布日期： 2024-05-19

- 新特性：
  - 支持使用 ServiceAccount 的 Secret Token 导入集群
  - 显示节点详情页、清空节点、暂停节点调度

## v4.0.0.0-alpha.02

发布日期： 2024-05-12

- 新特性：
  - 通过 webhook 接口，使用企业已有用户库中的用户名密码实现用户认证
  - 支持 OpenGauss 作为 Kuboard v4 的持久化存储
- 问题修正：
  - 修复不能打开容器日志界面、容器命令行界面的问题
  - 修复刷新页面后，出现 404 错误的问题

## v4.0.0.0-alpha.01

发布日期： 2024-04-06

- 使用 mysql / marida-db 作为 Kuboard v4 的持久化存储
- 实现最基本的 K8S 管理功能：
  - 通过 kubeconfig 导入集群
  - 管理集群各类型对象
  - 使用全量、增量、轮询等方式从 Kubernetes 集群同步对象到本地数据库
  - 实现 Kuboard 用户管理
    - 授权用户访问集群资源
